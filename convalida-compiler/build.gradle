apply plugin: 'java'

sourceCompatibility = JavaVersion.VERSION_1_7
targetCompatibility = JavaVersion.VERSION_1_7

dependencies {
    compile project(path: ':convalida-annotations')

    compile deps.autoService
    compile deps.javapoet
}